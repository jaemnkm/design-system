import styled from 'styled-components'
import PropTypes from 'prop-types'
import {
  space,
  themeGet
} from 'styled-system'
import {
  mapProps,
  applyVariations,
  getPaletteColor
} from './utils'

const {{componentName}} = mapProps(({ align, ...props }) => ({
  ...props
}))(styled.{{htmlTag}}`
  // Example: Use palette colors
  color: ${getPaletteColor('base')};

  // Advanced: Apply variation style along with custom styling from theme.componentStyles[component][variation]
  ${applyVariations('Text')}
`)

{{componentName}}.displayName = '{{componentName}}'

{{componentName}}.propTypes = {
}

export default {{componentName}}
